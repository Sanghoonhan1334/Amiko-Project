"use client"

// Removed development-only push tester UI. Keep an inert placeholder
// so imports elsewhere don't break until callers are cleaned up.
export function PushNotificationTester() {
  return null
}
